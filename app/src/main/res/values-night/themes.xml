<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@color/green</item>
        <item name="colorOnSecondary">@color/white</item>
    </style>

    <!-- custom font tab layout android -->
    <style name="TabLayoutFont" parent="TextAppearance.Design.Tab">
        <item name="android:fontFamily">@font/roboto_medium</item>
        <item name="android:textSize">16sp</item>
        <item name="textAllCaps">false</item>
    </style>
    <style name="TextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/bg_text_input_layout</item>
        <item name="boxStrokeWidth">1dp</item>
    </style>
</resources>